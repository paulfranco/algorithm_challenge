// Only Keep the last few
// Given an array and number X, remove all exceprt the last X elements, and return the array (changed and shorter) 
// Given ([2,4,6,8,10], 3), change the array to [6,8,10] and return it.

